{
  "name": "fyjc-tts-proxy",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Minimal proxy that holds the Azure Speech key server-side and forwards text-to-speech requests, so the browser never sees the API key.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
